---
name: Task (Child Issue)
about: タスク分解から生成される子 Issue のテンプレート
labels: feature
---

<!-- このファイルを `.github/ISSUE_TEMPLATE/task-child.md` にコピーして使用する -->

## 概要

[このタスクで実装・変更する内容を 1〜2 文で説明する]

## 変更ファイル

| 操作   | ファイルパス |
| ------ | ------------ |
| CREATE | path/to/file |
| MODIFY | path/to/file |

## 実装メモ

[設計上の制約・注意点・採用するアプローチの根拠を記載する。例: 既存の `UserRepository` を継承せず直接 `db` を呼び出す設計とした。理由はトランザクション境界を呼び出し元が制御する必要があるため。]

## 実装要件

- [何をすべきか（What）を箇条書きで列挙する。コードスニペットや How は原則含めない]

## 依存関係

[先に完了していなければならない Issue を記載する。なければ「なし」]

## 受入基準

- [ ] 実装が完了している
- [ ] テストが追加・更新されている
- [ ] レビュアーが変更内容を理解できる

## 背景・なぜこの実装が必要か

[この変更がなぜ必要か、どのような問題を解決するかを 1〜2 文で説明する。例: 既存の認証フローでは OAuth トークンの有効期限が考慮されておらず、期限切れトークンでも API が通ってしまうため修正が必要。]

## 実装ヒント

- [実装を始める前に読むべきファイルや関数を示す。例: `src/auth/token.ts` の `verifyToken()` がトークン検証の起点になる]
- [落とし穴や注意点を記載する。例: キャッシュ層をバイパスしないと古いトークンが再利用される可能性がある]
- [採用すべきパターンや設計指針を記載する。例: 既存の `middleware/` ディレクトリのパターンに合わせてクラスを作成する]

## 参考リンク

- [関連する PR や Issue の URL を記載する。例: #42 （元の認証実装）]
- [参照すべき外部ドキュメントや仕様書の URL を記載する。例: https://datatracker.ietf.org/doc/html/rfc6749 （OAuth 2.0 仕様）]
- [参考にすべきコードファイルのパスを記載する。例: `src/auth/` ディレクトリ全体]

## 実装前チェックリスト

- [ ] 依存する Issue がすべて完了していることを確認した
- [ ] 変更ファイルの現在の実装を読み、影響範囲を把握した
- [ ] テスト戦略（単体 / 統合 / E2E）を決定した
- [ ] レビュアーに伝えるべき設計上のトレードオフを整理した
